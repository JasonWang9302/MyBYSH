<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
	<!-- 使用通配符建议设置 成False -->
	<constant name="struts.enable.DynamicMethodInvocation" value="false"></constant>

	<package name="default" namespace="/" extends="struts-default">
		
		<!-- 定义新的拦截器栈, 配置 prepare 拦截器栈的 alwaysInvokePrepare 参数值为 false -->
		<interceptors>
			<interceptor-stack name="sshStack">
				<interceptor-ref name="paramsPrepareParamsStack">
					<param name="prepare.alwaysInvokePrepare">false</param>
				</interceptor-ref>
			</interceptor-stack>
		</interceptors>
		<!-- 使用新的拦截器栈  设置默认拦截器栈为上面定义的-->
		<default-interceptor-ref name="sshStack"></default-interceptor-ref>
        
		<!-- <global-allowed-methods>regex:.*</global-allowed-methods> -->

		<!-- <action name="index">
			<result>index.jsp</result>
		</action>
		<action name="employee_*" class="employeeAction" method="{1}">
			<result name="all">/WEB-INF/view/employeeList.jsp</result>
			<result name="toAdd">/WEB-INF/view/employee-toAdd.jsp</result>
			<result name="toEdit">/WEB-INF/view/employee-toEdit.jsp</result>
			
			<result type="stream" name="delete">
				<param name="contentType">text/html</param>
				<param name="inputName">inputStream</param>
			</result>
			<result type="stream" name="verifyName">
				<param name="contentType">text/html</param>
				<param name="inputName">inputStream</param>
			</result>
			<result name="add" type="redirectAction">employee_all</result>
			<allowed-methods>all,delete,toAdd,add,verifyName,toEdit,edit</allowed-methods>
		</action> -->


	</package>

</struts>


